node_modules/
dist/
dev-dist/
.DS_Store

# Screenshots and files delivered through the Claude desktop app land here.
# Useful locally, not part of the project.
Claude outputs/

# Generated by `node content/build.mjs` — the data/ files are the source.
app/src/content/bundle.json

# Art-only reference SVGs, regenerated by `node tools/build-diagrams.mjs --refs`.
# The .png files beside them ARE committed — those are what the image model is
# given, and making them needs a browser.
tools/refs/*.svg

transfer/
